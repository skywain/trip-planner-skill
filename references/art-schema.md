# art.json schema — pointer

The authoritative contract for `<plan>.art.json` (common block, every theme block, the
asset lookup order, the gen/sheet recipe) is **`themes/ART-SCHEMA.md`** — one copy only,
kept beside the renderers so it cannot drift. Read it there; the manual around it is
`references/themes.md`.
